The file 0-reset_to_98.c  contains function that takes a pointer to an int

The file 1-swap.c contains a function that swaps the values of two integers.

The file 2-strlen.c contains a function that returns the length of a string.

The file 3-puts.c contains a function that prints a string, followed by a new line, to stdout.

The file 4-print_rev.c contains a function that prints a string, in reverse, followed by a new line

The file 5-rev_string.c contains a function that reverses a string. 

The file 6-puts2.c contains a function that prints every other character of a string, starting with the first character, followed by a new line.

The file 7-puts_half.c contains a function that prints half of a string, followed by a new line.

The file 8-print_array.c contains a function that prints n elements of an array of integers, followed by a new line.

The file 9-strcpy.c a function that copies the string pointed to by src, including the terminating null byte (\0), to the buffer pointed to by dest.

The file 100-atoi.c contains a  a function that convert a string to an integer.

The file 101-keygen.c creates a program that generates random valid passwords for the program 101-crackme.
