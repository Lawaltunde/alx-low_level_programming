The file 0-strcat.c contains a function that concatenates two strings.

The file 1-strncat.c contains a function that concatenates two strings.

The file 2-strncpy.c contains  a function that copies a string.

The file 3-strcmp.c contans a function that compares two strings.

The file 4-rev_array.c contains a function that reverses the content of an array of integers.

The file 5-string_toupper.c contains a function that changes all lowercase letters of a string to uppercase.

The file 6-cap_string.c contains  a function that capitalizes all words of a string.

The file 7-leet.c contains a function that encodes a string into 1337.

The file 100-rot13.c contains a function that encodes a string using rot13.

The file 101-print_number.c contains  a function that prints an integer.

The file 102-magic.c contains a the program prints a[2] = 98, followed by a new line.

The file 103-infinite_add.c contains a function that adds two numbers.

The file 104-print_buffer.c a function that prints a buffer.
