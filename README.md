My first C project in alx.

The directory 0x00-hello_world contains files with C project.

The directory 0x01-variables_if_else_while contains  C - Variables, if, else, while loop project.

The directory 0x02-functions_nested_loops contains nested loop C project.

The directory 0x03-debuggin contains debuggin projects.

The directory 0x04-more_functions_nested_loops contains More functions, more nested loops projects.

The directory 0x05-pointers_arrays_strings contains C - Pointers, arrays and strings projects.

The directory 0x06-pointers_arrays_strings contains 0x06. C - More pointers, arrays and strings projects.


