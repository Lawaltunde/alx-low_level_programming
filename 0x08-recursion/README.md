The 0-puts_recursion.c contains function that prints a string, followed by a new line.

The file 1-print_rev_recursion.c contains a function that prints a string in reverse.

The file 2-strlen_recursion.c contains a function that returns the length of a string.

The file 3-factorial.c contains a function that returns the factorial of a given number.

The file 4-pow_recursion.c contains a function that returns the value of x raised to the power of y.

The file 5-sqrt_recursion.c contains a function that returns the natural square root of a number.

The file 6-is_prime_number.c contains a function that returns 1 if the input integer is a prime number, otherwise return 0.

The file 100-is_palindrome.c contains a function that returns 1 if a string is a palindrome and 0 if not.

The file 101-wildcmp.c contains a function that compares two strings and returns 1 if the strings can be considered identical, otherwise return 0.
