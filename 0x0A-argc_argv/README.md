The file 0-whatsmyname.c contains a program that prints its name, followed by a new line.

The file 1-args.c contains a program that prints the number of arguments passed into it.

The file 2-args.c a program that prints all arguments it receives.

The file 3-mul.c contains a program that multiplies two numbers.

The file 4-add.c a program that adds positive numbers.

The file 100-change.c a program that prints the minimum number of coins to make change for an amount of money.
