#The file 0-preprocessor creates  a script that runs a C file through the preprocessor and save the result into anothe
#The file 1-compiler creates a script that compiles a C file but does not link
#The file 2-assembler creates a script that generates the assembly code of a C code and save it in an output file. 
#The file 3-name creates a script that compiles a C file and creates an executable named cisfun

