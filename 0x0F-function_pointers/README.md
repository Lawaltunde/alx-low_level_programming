The file 0-print_name.c contains  a function that prints a name.

The file 1-array_iterator.c contains a function that executes a function given as a parameter on each element of an array.

The file 2-int_index.c contains  function that searches for an integer.

The files 3-main.c, 3-op_functions.c, 3-get_op_func.c, 3-calc.h contain  programs a program that performs simple operations.

The file 100-main_opcodes.c contains a program that prints the opcodes of its own main function.
