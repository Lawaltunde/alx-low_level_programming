The file 0-malloc_checked.c contains a function that allocates memory using malloc.

The file 1-string_nconcat.c contains a function that concatenates two strings.

The file 2-calloc.c contains a  function that allocates memory for an array, using malloc.

The file 3-array_range.c contains a function that creates an array of integers.

The file 100-realloc.c contains a function that reallocates a memory block using malloc and free.

The file 101-mul.c contains a program that multiplies two positive numbers.
