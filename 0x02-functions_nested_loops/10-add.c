#include <stdio.h>
#include "main.h"
/**
  * add - function that adds two integers and returns the result
  * @c: argument
  * @b: argument
  * Return: addition of two numbers
  */
int add(int c, int b)
{
	return (c + b);
}
