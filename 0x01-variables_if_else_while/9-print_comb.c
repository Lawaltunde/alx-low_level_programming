#include <stdio.h>
/**
 * main - Print possible combination of single digit.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
	for (i = 48; i  < 58; j++)
	{
		putchar(i);
	}
		putchar(',');
	}
	putchar('\n');
	return (0);
}
