The file 0-binary_to_uint.c contains a function that converts a binary number to an unsigned int.

The file 1-print_binary.c contains a function that prints the binary representation of a number.

The file 2-get_bit.c contains a function that returns the value of a bit at a given index.

The file 3-set_bit.c contains a function that sets the value of a bit to 1 at a given index.

The file 4-clear_bit.c contains a function that sets the value of a bit to 0 at a given index.

The file 5-flip_bits.c contains a function that returns the number of bits you would need to flip to get from one number to another.

The file 100-get_endianness.c contains a function that checks the endianness.

Finding of password of a particular program.
