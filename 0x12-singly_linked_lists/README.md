The file 0-print_list.c contains  a function that prints all the elements of a list_t list.

The file 1-list_len.c contains a function that returns the number of elements in a linked list_t list.

The file 2-add_node.c contains a function that adds a new node at the beginning of a list_t list.

The file 3-add_node_end.c contains a function that adds a new node at the end of a list_t list.

The file 4-free_list.c contains a function that frees a list_t list.

The file 100-first.c contains a function that prints You're beat! and yet, you must allow,\nI bore my house upon my back!\n before the main function is executed.

The file 101-hello_holberton.asm contains 64-bit program in assembly that prints Hello, Holberton, followed by a new line.


